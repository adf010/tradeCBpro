LIST PORTFOLIOS

Get all portfolios of a user.

Portfolio Type choices are: UNDEFINED ; DEFAULT ; CONSUMER ; INTX

The default is DEFAULT so you can just accept the default by pressing ENTER or enter other TYPE.


